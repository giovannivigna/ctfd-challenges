# webifile

Flask web challenge that contains a directory traversal vulnerability.

## Local run

From this directory:

- `./scripts/build.sh`
- `./scripts/run.sh` (listens on port `8862`)
- `./scripts/stop.sh`
