service popenepop
{
  disable = no
  type = UNLISTED
  wait = no
  server = /home/challenge/ro/popenepop
  socket_type = stream
  protocol = tcp
  # Run as challenge user (non-root); can read /flag but not modify it
  user = 31337
  port = 7731
  flags = REUSE
}