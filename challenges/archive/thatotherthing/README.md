# thatotherthing

An angr-friendly symbolic execution challenge.

The service reads a single line, runs it through a deliberately annoying validation function, and prints `/flag` if the input is correct.

Players are provided only a statically-linked compiled binary (with symbols, no debug info).
