# secretword2

Very simple challenge (extension of secretword) requiring to extract a static secret (without the source).
Make sure that the port in the Dockerfile and in src/xinetd.conf are the same.
